# 0043 ユーザー手順書（あなたがやること）

日付：2026-05-23 ／ ブランチ：`feat/magi-rebuild`

## ユーザーが与えた指示
- 「俺のタスクをリスト化して手順にして」。

## 実施内容
- `docs/USER_RUNBOOK.md` を作成：ユーザー操作・判断を実行順の手順書に。
  フェーズ0準備→1動作確認(backtest/朝バッチ)→2自動化(launchd)→3ペーパー運用→4実弾(増額ゲート)。
  ＋「決めてくれれば私が実装する項目」「常時効く安全装置」。
- 各ステップに実コマンドを併記。発注/決裁は moomoo 手動（Tier1）を明記。

## コミット
- 本md＋USER_RUNBOOK.md＋progress/README を同一コミット（doc）。
