# 0053 — architecture.html にペーパー運用パイプラインを反映（完成後の姿）

## ユーザーが与えた指示
- テストまで推奨で実装し、**完成後のアーキテクチャを反映して**。

## 実施内容
- `architecture.html`：
  - §0 コミットログに 0049(姿勢3パターン)/0050(P4-1紙執行)/0051(GENDO推奨カード)/0052(P4-3一気通貫) を追記（✅）。
  - 凡例に「ペーパー運用パイプライン接続」を明記：universe→収集→**守りレンズ(検品)**→MAGI(株価は投票外)→**GENDO推奨カード**(守り主導・攻めは灰色)→**人間決裁**→**翌寄り紙約定**(B')→評価。攻めは昇格まで枠0・守りは論理固定で最適化しない。実行=`scripts/run_paper.py`。
  - 最終更新行を更新（目的再定義＋ペーパー接続）。SVGは非改変。

## 検証
- tests/unit 全 green（HTML変更・コード非改変）。

## コミット / 状態 / 次
- コミット：本md と同コミット。
- 状態：**ペーパー運用まで end-to-end が接続し、アーキテクチャに反映済**（監視→GENDO推奨→決裁→紙約定→評価）。
- 残（次サイクル）：P4-4 評価の向け直し（守りはリターンで測らない＝質パッシブ追随＋プロセス遵守）／攻め昇格条件の実装（前向き較正＋コントロール参戦）／P4-2 サテライト枠の永続化。
