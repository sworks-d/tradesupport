# Claude Code への最初の指示（コピペ用）

このファイルは **Claude Code に渡す最初のプロンプト**。

設計フェーズは完了済み。設計書は `docs/` ディレクトリにまとめて格納される。
このプロンプトを Claude Code に渡すことで実装フェーズが始まる。

---

# 📋 ユーザー側の事前準備

Claude Code に渡す前に、以下を確認してください：

## 1. ディレクトリ構造を作る

新規プロジェクトディレクトリを用意し、`docs/` 配下に設計書を全て格納：

```
~/Projects/trading-agent/
└── docs/
    ├── README_INDEX.md              # 全体ナビゲーション
    ├── CLAUDE_CODE_INSTRUCTIONS.md  # マスター指示書
    ├── IMPLEMENTATION_PHASES.md     # タスクリスト
    ├── STEP_A_FINAL.md              # 要件定義
    ├── STEP_B_FINAL.md              # UI設計
    ├── PANEL_SPECS.md               # C-1：パネルロジック
    ├── SYSTEM_DESIGN.md             # C-2：システム設計
    ├── AGENT_SPECS.md               # C-3：エージェント設計
    ├── ORCHESTRATION.md             # C-4：オーケストレーション
    ├── OPERATIONS.md                # C-5：運用
    ├── architecture.html            # システム全体図
    └── dashboard.html               # UI 完成形
```

## 2. Claude Code を起動

```bash
cd ~/Projects/trading-agent
claude  # Claude Code を起動
```

## 3. 下記のプロンプトをコピペ

下の `==== ここからプロンプト ====` から `==== ここまでプロンプト ====` の間を全選択してコピー → Claude Code に貼り付け。

---

==== ここからプロンプト ====

これから「Trading Agent」というプロジェクトの実装を進めてもらいます。

## まずやってほしいこと

`docs/` ディレクトリに 12 個の設計書ファイルがあります。
**最初に以下を必ず実行してください：**

1. **`docs/CLAUDE_CODE_INSTRUCTIONS.md` を読む**
   これがあなた向けのマスター指示書です。実装の原則、守ること、やってはいけないこと、設計書の読み方、コーディング規約、ユーザーへの報告形式、すべて書いてあります。

2. **`docs/IMPLEMENTATION_PHASES.md` を読む**
   フェーズ別のタスクリストです。Phase 1.0 から順に着手します。各タスクの完了基準・依存関係・想定セッション数が定義されています。

3. **`docs/STEP_A_FINAL.md` を読む**
   プロジェクトの要件定義です。何を作るか、なぜ作るか、コンセプトを理解してください。

4. **`docs/dashboard.html` をブラウザで開いて触る**
   UI の完成形です。実際に開いて、各パネルの構造・インタラクションを体感してください。設計書だけでは分からない「狙い」が見えます。

## 重要な前提

このプロジェクトは設計フェーズに **27時間以上のブレスト** をかけています。設計書は迷い、議論、方向転換、合意の積み重ねの結果です。

- **設計書を信じる、勝手に解釈しない**
- **論理は設計書、コードはあなたの責任**
- **Phase 1 のスコープを超えない**（Phase 2- の機能は先回り実装禁止）
- **仮設定値は必ず変更可能に**（settings テーブル経由）
- **エラー時も止まらない設計**（Graceful Degradation）

これらの原則は `CLAUDE_CODE_INSTRUCTIONS.md` セクション 1 に詳細があります。

## 最初に作るべきもの

`IMPLEMENTATION_PHASES.md` の **Phase 1.0：環境構築** から着手してください。

- Task 1.0.1：プロジェクト初期化（pyproject.toml）
- Task 1.0.2：設定モジュール（config.py）
- Task 1.0.3：ロギング基盤（structlog）
- Task 1.0.4：データベース初期化（init_db.py）

## あなたが守る原則

`CLAUDE_CODE_INSTRUCTIONS.md` セクション 1.1 の5原則：

1. **設計書を信じる、勝手に解釈しない**
2. **論理は設計書、コードはあなたの責任**
3. **Phase 1 のスコープを超えない**
4. **仮設定値は必ず仮として実装**（ハードコード禁止）
5. **エラー時も止まらない設計**

## 進捗管理

`docs/PROGRESS.md` を新規作成し、各タスクの進捗を記録してください。各セッション開始時に確認、終了時に更新してください。

## 報告形式

各タスク完了時、以下の形式で報告してください：

```
✅ 完了：[何ができるようになったか]
📋 動作確認方法：[具体的なコマンド or URL]
⚠️ 注意点：[あれば]
🔄 次のタスク：[次は何をするか]
```

質問が必要な場合の形式：

```
❓ 質問：[判断が必要な事項]
📚 関連設計書：[どの設計書のどこを見たか]
💭 私の解釈：[現時点での解釈]
🎯 選択肢：
  - A. [選択肢A] — メリット・デメリット
  - B. [選択肢B] — メリット・デメリット
👉 推奨：[私が推す選択肢と理由]
```

## ユーザーが事前に決めたこと（朝の確認結果）

設計書冒頭の「📋 朝の確認用」セクションに論点が整理されていますが、以下はユーザーが「推奨で進めてOK」と決定済みです：

- 利確スコアの「シナリオ達成度」判定 → thesis_checklist + LLM ハイブリッド
- screening Universe → 時価総額上位500 + 流動性しきい値
- 目標期間の決定 → デフォルト提示 + ユーザー承認
- 利確は半量売却を選択肢化、損切りは全量強制
- DB は SQLite 一本（Phase 1）
- ORM は SQLModel
- LangChain ベースで実装
- 自作 DAG エンジン（LangGraph は使わない）
- macOS 専用、Docker 化しない、uv + launchd

詳細はそれぞれの設計書を参照。

## 最初の応答

このプロンプトを受け取ったら、まず以下を出力してください：

1. `CLAUDE_CODE_INSTRUCTIONS.md`、`IMPLEMENTATION_PHASES.md`、`STEP_A_FINAL.md` を読んだ確認
2. プロジェクトの一文要約をあなたの言葉で
3. これから着手する Phase 1.0 のタスクリスト
4. 着手前に確認したいことがあれば質問

質問がなければ、ユーザーの「進めて」の確認を得てから Task 1.0.1 に着手してください。

それでは始めましょう。

==== ここまでプロンプト ====

---

# 📌 ユーザー向けの補足（プロンプト送信後）

## Claude Code が最初に返してくる内容（想定）

```
こんにちは、Claude Code です。

📚 確認しました：
- CLAUDE_CODE_INSTRUCTIONS.md
- IMPLEMENTATION_PHASES.md
- STEP_A_FINAL.md

🎯 プロジェクト要約：
moomoo OpenAPI を介して米国株・日本株の中期投資を AI マルチエージェントが
支援する、ローカル完結型の個人向けトレードアシスタント。
朝5分でダッシュボードを確認し、売り買いレコメンドを判断するワークフロー。

📋 これから着手する Phase 1.0 のタスク：
- Task 1.0.1：プロジェクト初期化（pyproject.toml）
- Task 1.0.2：設定モジュール（config.py）
- Task 1.0.3：ロギング基盤（structlog）
- Task 1.0.4：データベース初期化（init_db.py）

❓ 着手前の確認：
（何か質問があれば）

進めていいですか？
```

このような返答が来たら、確認して「進めて」と返す。

## 注意点

### 想定外の動きをしたら

- 「設計書通りか確認して」と促す
- 該当する設計書のセクションを指摘
- 必要なら CLAUDE_CODE_INSTRUCTIONS.md セクション 1.1 の5原則を再掲

### Phase 1 スコープを超えた提案が来たら

- 「それは Phase 2 以降の機能です」と明確に拒否
- `CLAUDE_CODE_INSTRUCTIONS.md` セクション 1.2「やってはいけないこと」を参照
- `README_INDEX.md` の「Phase 1 で『やらないこと』リスト」を参照

### 進捗が遅いと感じたら

- `docs/PROGRESS.md` を Claude Code に確認させる
- 「今どのタスクか」「ブロッカーは何か」を聞く
- 必要なら粒度を小さくする（Task 1.4.3 → 1.4.3.a / 1.4.3.b）

### コストが想定より高くなったら

- Claude Code に「設計時間 vs 実装時間」のバランスを確認
- 各タスクの想定セッション数（IMPLEMENTATION_PHASES.md）と実績を比較
- 必要なら一旦止めて方針再確認

---

# 📊 完了の目安

## Phase 1.0（環境構築）の完了サイン

- `uv sync` で全依存パッケージがインストールできる
- `.env` をコピーしてバリデーションが通る
- `uv run python scripts/init_db.py` で空の DB が作成できる
- ロガーが動作する

## Phase 1 全体の完了サイン

- 朝バッチが毎日動く（JST 5:00）
- ダッシュボードが http://localhost:8000 で表示される
- moomoo 連携（読み取り）が動作
- 月次 API コスト ¥5,000 以内
- 4 週間の運用実績

`OPERATIONS.md` セクション 11 のチェックリストを満たせば Phase 2 検討。

---

# 🚨 緊急時の対処

実装中に深刻な問題が発生した場合：

## Claude Code が暴走している

- まず止める：「stop」と入力
- Git で全コミットを確認、必要なら revert
- 直前のコミットに戻る：`git reset --hard HEAD~1`

## 設計書と矛盾するコードが生まれた

- Claude Code に該当箇所を指摘
- どちらが正しいかを確認
- 設計書側が正しいなら、コードを修正

## API コストが急増

- `~/.trading-agent/HALT` ファイルを作成（緊急停止）
- cost_logs を確認
- Claude Code に原因調査を依頼

`OPERATIONS.md` セクション 8（緊急停止フロー）を参照。

---

# 🎯 このプロジェクトの哲学（最後に）

1. **論理は詳細、コードは自由**
2. **Phase 1 の完成を最優先**
3. **データに語らせる**
4. **規律 > 感情**
5. **透明性を保つ**
6. **学習は副産物**

これを Claude Code にも、自分自身にも、忘れずに。

---

それでは、実装フェーズの始まりです。良い旅を。
